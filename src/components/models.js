const User = require('./user/user.model');
const Reward = require('./reward/reward.model');
const Claim = require('./claim/claim.model');
const Turn = require('./turn/turn.model');
const History = require('./history/history.model');
const Player = require('./player/player.model');

module.exports = {
  User,
  Reward,
  Claim,
  Turn,
  History,
  Player,
};
